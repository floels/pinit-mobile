import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  profilePicture: {
    width: 24,
    height: 24,
    borderRadius: 12,
  },
  focusedProfilePictureContainer: {
    alignItems: "center",
    justifyContent: "center",
    borderWidth: 1,
    borderColor: "black",
    width: 28,
    height: 28,
    borderRadius: 14,
  },
});

export default styles;
