import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    paddingTop: 80,
  },
  closeButton: {
    marginLeft: 20,
    marginBottom: 40,
  },
});

export default styles;
